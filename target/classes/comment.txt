1、流程状态如下： 待审批，审批中（在各个节点中流转时的状态），  审批通过（所有节点审批通过）  ， 被驳回（任何一个节点驳回） ，归档（部分流程存在此状态），关闭（仅发起人有次权限）；

2、流程单个节点：未读、已读、已审批（驳回，通过）

3、上传附件时候图片进行压缩


小程序Secret:472214f356a074f7c8fe1f56ca523c87
			

文件服务器为单独部署的服务，用于文件格式转换和文件预览  路径为  flow/用户ID/YYYY-MM-DD/流程实例ID/文件名.docx或者doc，PDF

云盘文件服务路径为  cloud/用户ID/YYYY-MM-DD/文件名.docx或者doc，PDF

解决nginx跨域请求报错问题，在nginx.conf下，location / {}里，加入 add_header 'Access-Control-Allow-Origin' *;
           add_header 'Access-Control-Allow-Credentials' 'true';add_header 'Access-Control-Allow-Methods' 'GET';
*处可设置为具体的域名

processFilePath为上传的bpmn文件zip存放路径    路径具体格式为：资质类别ID/YYYY-MM-DD/上传人ID/文件名.zip

//站内公告，站内新闻相关图片  上传路径  news/日期（YYYY-MM-DD）/上传人ID/图片名.图片格式  配置写到ueditor的配置文件里

站内公告，站内新闻主图  上传路径   news/日期（YYYY-MM-DD）/上传人ID/图片名.图片格式   配置到base.properties里

站内公告，文本图片上传相关设置：默认在网站根目录路径下，在/webSocket/WebRoot/public/ueditor-1.4.3/jsp/config.json文件下修改imagePathFormat（上传保存路径）属性配置时前边加上"../"使得图片保存路径与项目文件同一层次，方便以后管理
同时，应在imageUrlPrefix（访问路径前缀）添加当前环境地址
具体路径为news/日期（YYYY-MM-DD）/上传人ID/图片名.图片格式

加入顺发或者并发 抢发后的流程列表查询  变成了 ：
1,先得到整体流程状态 非完成/关闭  非驳回    2,当前节点定义的状态（是顺发，并发还是抢发）  3,审批人的状态   4. 当前节点其他审批人的状态（上一个人状态（判断是不是走到当前） 下一个人状态（判断是不是最后一个人））

在base.propertis文件中加入小程序消息提醒页面消息内容，使用","进行分割成数组，然后在页面进行穿插显示。

message.flow1 “我发起的流程被审批通过”消息拼接
message.flow2 “待我审批的流程”消息拼接
message.flow3 “我的流程被驳回到本人“消息拼接
message.flow4 “我处理过的流程驳回到我”消息拼接